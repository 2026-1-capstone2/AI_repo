fastapi==0.110.0
uvicorn[standard]==0.27.0
pydantic>=2.10,<3        # Python 3.13 호환 wheel 제공 (2.10 이상)
pydantic-settings>=2.6,<3
python-dotenv==1.0.0

# numpy 1.x 고정: torch 2.1.1 (실모델) wheel 이 numpy 1.x 빌드라서
# base 단계에서 2.x 가 끌려오면 설치 도중 잠깐 깨진 환경이 노출된다.
# stub 환경에서도 무해하므로 base 에 명시.
numpy<2

# RabbitMQ
pika==1.3.2

# HTTP client (영상 다운로드)
httpx==0.27.0

# S3 (spatial_features 저장)
boto3==1.34.0

# 실모델(stub_models=False) 추가 의존성은 requirements-ml.txt 에 분리되어 있음
# (Python 3.10 + CUDA 12.1 환경에서만 설치 가능. stub 모드에서는 미설치 권장)

# ngrok (개발용 터널)
pyngrok==7.0.0
